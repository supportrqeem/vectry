# 🔥 دليل إعداد Firebase خطوة بخطوة
# Firebase Setup Guide - Step by Step

---

## 📋 المحتويات / Contents

1. [إنشاء مشروع Firebase](#1-إنشاء-مشروع-firebase)
2. [تفعيل Authentication](#2-تفعيل-authentication)
3. [إعداد Firestore Database](#3-إعداد-firestore-database)
4. [الحصول على إعدادات التكوين](#4-الحصول-على-إعدادات-التكوين)
5. [تحديث ملفات المشروع](#5-تحديث-ملفات-المشروع)
6. [نشر المشروع](#6-نشر-المشروع)
7. [استكشاف الأخطاء](#7-استكشاف-الأخطاء)

---

## 1. إنشاء مشروع Firebase

### الخطوة 1.1: الذهاب إلى Firebase Console
1. افتح المتصفح واذهب إلى: **https://console.firebase.google.com/**
2. سجل الدخول بحساب Google الخاص بك

### الخطوة 1.2: إنشاء مشروع جديد
1. انقر على **"Create a project"** أو **"إنشاء مشروع"**
2. أدخل اسم المشروع (مثال: `vehicle-evaluation-system`)
3. اختر ما إذا كنت تريد تفعيل Google Analytics (اختياري)
4. انقر **"Create Project"** وانتظر حتى يكتمل الإنشاء

### الخطوة 1.3: إضافة تطبيق ويب
1. في صفحة المشروع، انقر على أيقونة **الويب `</>`**
2. أدخل اسم للتطبيق (مثال: `Vehicle Evaluation Web`)
3. ✅ حدد **"Also set up Firebase Hosting"** إذا أردت استضافة الموقع على Firebase
4. انقر **"Register app"**
5. **⚠️ احفظ إعدادات التكوين** (ستحتاجها لاحقاً)

```javascript
// ستظهر لك إعدادات مشابهة لهذه:
const firebaseConfig = {
    apiKey: "AIzaSyC.....................",
    authDomain: "your-project.firebaseapp.com",
    projectId: "your-project",
    storageBucket: "your-project.appspot.com",
    messagingSenderId: "123456789",
    appId: "1:123456789:web:abc123..."
};
```

---

## 2. تفعيل Authentication

### الخطوة 2.1: الذهاب إلى قسم المصادقة
1. في القائمة الجانبية، انقر على **"Build"** ثم **"Authentication"**
2. انقر **"Get started"**

### الخطوة 2.2: تفعيل طرق تسجيل الدخول

#### ✉️ Email/Password:
1. انقر على **"Email/Password"**
2. فعّل **"Enable"**
3. (اختياري) فعّل **"Email link"** للدخول بدون كلمة مرور
4. انقر **"Save"**

#### 🔵 Google:
1. انقر على **"Google"**
2. فعّل **"Enable"**
3. أدخل **Project public-facing name** (اسم يظهر للمستخدمين)
4. اختر **Support email**
5. انقر **"Save"**

#### 🔷 Microsoft (اختياري):
1. انقر على **"Microsoft"**
2. فعّل **"Enable"**
3. ستحتاج إلى إنشاء تطبيق في **Azure Portal**:
   - اذهب إلى https://portal.azure.com/
   - أنشئ **App Registration**
   - احصل على **Application (client) ID** و **Client Secret**
4. أدخل البيانات في Firebase وانقر **"Save"**

### الخطوة 2.3: إعداد Authorized Domains
1. في قسم Authentication، انقر على **"Settings"**
2. انتقل إلى **"Authorized domains"**
3. تأكد من إضافة نطاقك (مثل: `your-domain.com`)
4. للتطوير المحلي، `localhost` مضاف تلقائياً

---

## 3. إعداد Firestore Database

### الخطوة 3.1: إنشاء قاعدة البيانات
1. في القائمة الجانبية، انقر على **"Build"** ثم **"Firestore Database"**
2. انقر **"Create database"**
3. اختر **الموقع** (مثال: `nam5 (us-central)` أو موقع أقرب لك)
4. اختر **"Start in test mode"** للبدء (سنغير القواعد لاحقاً)
5. انقر **"Create"**

### الخطوة 3.2: إعداد قواعد الأمان
1. في Firestore، انقر على **"Rules"**
2. استبدل القواعد بالكود التالي:

```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // المستخدمين
    match /users/{userId} {
      // القراءة والكتابة للمالك فقط
      allow read, write: if request.auth != null && request.auth.uid == userId;
      
      // المركبات
      match /vehicles/{vehicleId} {
        allow read, write: if request.auth != null && request.auth.uid == userId;
      }
    }
    
    // رفض كل شيء آخر
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
```

3. انقر **"Publish"**

### الخطوة 3.3: إنشاء Indexes (إذا لزم الأمر)
إذا ظهرت لك رسالة خطأ تطلب إنشاء index:
1. انقر على الرابط في رسالة الخطأ
2. سيتم توجيهك لإنشاء الفهرس تلقائياً
3. انقر **"Create index"**

---

## 4. الحصول على إعدادات التكوين

### الخطوة 4.1: نسخ الإعدادات
1. انقر على **⚙️ إعدادات المشروع** (الترس) في أعلى القائمة الجانبية
2. مرر للأسفل إلى قسم **"Your apps"**
3. انقر على تطبيق الويب الخاص بك
4. انسخ كائن `firebaseConfig`

### الخطوة 4.2: شكل الإعدادات النهائي
```javascript
const firebaseConfig = {
    apiKey: "AIzaSyBxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    authDomain: "your-project-id.firebaseapp.com",
    projectId: "your-project-id",
    storageBucket: "your-project-id.appspot.com",
    messagingSenderId: "123456789012",
    appId: "1:123456789012:web:abcdef123456"
};
```

---

## 5. تحديث ملفات المشروع

### الخطوة 5.1: تحديث auth.html
1. افتح ملف `auth.html`
2. ابحث عن:
```javascript
const firebaseConfig = {
    apiKey: "YOUR_API_KEY",
    ...
};
```
3. استبدل القيم بإعداداتك الحقيقية

### الخطوة 5.2: تحديث dashboard.html
1. افتح ملف `dashboard.html`
2. كرر نفس الخطوة - استبدل `firebaseConfig`

### الخطوة 5.3: مثال على التحديث
```javascript
// قبل
const firebaseConfig = {
    apiKey: "YOUR_API_KEY",
    authDomain: "YOUR_PROJECT_ID.firebaseapp.com",
    projectId: "YOUR_PROJECT_ID",
    storageBucket: "YOUR_PROJECT_ID.appspot.com",
    messagingSenderId: "YOUR_SENDER_ID",
    appId: "YOUR_APP_ID"
};

// بعد
const firebaseConfig = {
    apiKey: "AIzaSyBxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    authDomain: "vehicle-eval.firebaseapp.com",
    projectId: "vehicle-eval",
    storageBucket: "vehicle-eval.appspot.com",
    messagingSenderId: "123456789012",
    appId: "1:123456789012:web:abcdef123456"
};
```

---

## 6. نشر المشروع

### الخيار أ: Firebase Hosting (موصى به)

#### 6.1 تثبيت Firebase CLI
```bash
npm install -g firebase-tools
```

#### 6.2 تسجيل الدخول
```bash
firebase login
```

#### 6.3 تهيئة المشروع
```bash
firebase init hosting
```
- اختر المشروع الخاص بك
- حدد مجلد `public` كمجلد الموقع
- اختر **Yes** للـ Single-page app

#### 6.4 نسخ الملفات
انسخ الملفات التالية إلى مجلد `public`:
- `auth.html`
- `dashboard.html`
- أعد تسمية `auth.html` إلى `index.html`

#### 6.5 النشر
```bash
firebase deploy --only hosting
```

### الخيار ب: أي استضافة أخرى
1. ارفع الملفات إلى خادمك
2. تأكد من إضافة نطاقك إلى **Authorized Domains** في Firebase

---

## 7. استكشاف الأخطاء

### ❌ خطأ: "auth/configuration-not-found"
**السبب**: إعدادات Firebase غير صحيحة
**الحل**: تحقق من `firebaseConfig` وتأكد من صحة القيم

### ❌ خطأ: "auth/unauthorized-domain"
**السبب**: النطاق غير مصرح
**الحل**: أضف نطاقك إلى Authentication > Settings > Authorized domains

### ❌ خطأ: "permission-denied" في Firestore
**السبب**: قواعد الأمان
**الحل**: تحقق من قواعد Firestore وتأكد من صحتها

### ❌ خطأ: "auth/popup-blocked"
**السبب**: المتصفح يحجب النوافذ المنبثقة
**الحل**: السماح بالنوافذ المنبثقة أو استخدام signInWithRedirect

### ❌ خطأ: "auth/network-request-failed"
**السبب**: مشكلة في الاتصال
**الحل**: تحقق من الاتصال بالإنترنت وحاول مجدداً

---

## 📝 ملاحظات مهمة

### 🔒 الأمان
- **لا تشارك** قيم `firebaseConfig` في مستودعات عامة
- استخدم **Environment Variables** للقيم الحساسة في الإنتاج
- راجع **قواعد الأمان** بانتظام

### 💰 التكلفة
- خطة **Spark (المجانية)** تكفي للبدء
- الحدود: 50K قراءة/يوم، 20K كتابة/يوم، 20K حذف/يوم
- راقب استخدامك في Firebase Console

### 🔄 النسخ الاحتياطي
- فعّل النسخ الاحتياطي التلقائي في Firestore
- استخدم ميزة التصدير في التطبيق بانتظام

---

## 🎉 تهانينا!

بعد إكمال هذه الخطوات، سيكون لديك:
- ✅ نظام تسجيل دخول آمن
- ✅ قاعدة بيانات سحابية
- ✅ مزامنة فورية بين الأجهزة
- ✅ بيانات محمية لكل مستخدم

---

## 📞 الدعم

- 📚 [Firebase Documentation](https://firebase.google.com/docs)
- 💬 [Firebase Community](https://firebase.google.com/community)
- 🐛 [Stack Overflow](https://stackoverflow.com/questions/tagged/firebase)

---

**آخر تحديث**: ديسمبر 2024
