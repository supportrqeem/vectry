# 🔧 دليل إعداد Supabase لنظام تقييم المركبات

## 📋 المتطلبات

- حساب Supabase (مجاني أو مدفوع)
- متصفح ويب حديث
- معلومات المشروع:
  - **Project URL**: `https://enrlrqjsgcpcggyuoazr.supabase.co`
  - **Anon Key**: موجود في ملف `js/supabase-config.js`

---

## 🚀 خطوات الإعداد

### الخطوة 1: تسجيل الدخول إلى Supabase Dashboard

1. اذهب إلى [app.supabase.com](https://app.supabase.com)
2. سجل الدخول بحسابك
3. اختر المشروع: `enrlrqjsgcpcggyuoazr`

---

### الخطوة 2: إنشاء جداول قاعدة البيانات

1. من القائمة الجانبية، اختر **SQL Editor**
2. انقر على **New Query**
3. انسخ محتوى ملف `supabase-setup.sql` والصقه
4. انقر على **Run** لتنفيذ الاستعلام

> ⚠️ **ملاحظة مهمة**: تأكد من تنفيذ الملف كاملاً لإنشاء جميع الجداول والسياسات الأمنية

---

### الخطوة 3: إعداد Storage (تخزين الصور)

1. من القائمة الجانبية، اختر **Storage**
2. انقر على **New Bucket**
3. أدخل الاسم: `vehicle-images`
4. ✅ اختر **Public bucket** (للسماح بالوصول العام للصور)
5. انقر على **Create bucket**

#### إعداد سياسات Storage:

1. انقر على **vehicle-images** bucket
2. اذهب إلى تبويب **Policies**
3. أضف السياسات التالية:

**سياسة القراءة (للجميع):**
```sql
CREATE POLICY "Public can view images"
ON storage.objects FOR SELECT
USING (bucket_id = 'vehicle-images');
```

**سياسة الرفع (للمستخدمين المسجلين):**
```sql
CREATE POLICY "Users can upload images"
ON storage.objects FOR INSERT
WITH CHECK (
  bucket_id = 'vehicle-images' 
  AND auth.role() = 'authenticated'
);
```

**سياسة الحذف (للمالك فقط):**
```sql
CREATE POLICY "Users can delete own images"
ON storage.objects FOR DELETE
USING (
  bucket_id = 'vehicle-images' 
  AND auth.role() = 'authenticated'
);
```

---

### الخطوة 4: إعداد المصادقة (Authentication)

#### تمكين تسجيل الدخول بالبريد الإلكتروني:

1. من القائمة الجانبية، اختر **Authentication**
2. اذهب إلى **Providers**
3. تأكد من تفعيل **Email** provider
4. في إعدادات Email:
   - ✅ Enable email confirmations (اختياري)
   - ✅ Enable email change confirmations (اختياري)

#### تمكين تسجيل الدخول بـ Google (اختياري):

1. اذهب إلى [Google Cloud Console](https://console.cloud.google.com)
2. أنشئ OAuth 2.0 Client ID
3. في Supabase:
   - اذهب إلى **Authentication** > **Providers** > **Google**
   - أضف Client ID و Client Secret
   - ✅ فعّل Google provider

---

### الخطوة 5: إعداد Site URL

1. من **Authentication** > **URL Configuration**
2. حدث **Site URL** إلى رابط موقعك المنشور
3. أضف **Redirect URLs** المسموح بها:
   ```
   https://your-domain.com/dashboard.html
   https://your-domain.com/index.html
   ```

---

## 🧪 اختبار الإعداد

### اختبار قاعدة البيانات:

1. اذهب إلى **Table Editor**
2. تأكد من وجود الجداول التالية:
   - `vehicles` ✅
   - `warehouses` ✅
   - `evaluators` ✅
   - `activities` ✅

### اختبار Storage:

1. اذهب إلى **Storage**
2. تأكد من وجود bucket: `vehicle-images` ✅

### اختبار المصادقة:

1. افتح الموقع في المتصفح
2. جرب إنشاء حساب جديد
3. جرب تسجيل الدخول
4. تأكد من إعادة التوجيه للوحة التحكم ✅

---

## 🔍 استكشاف الأخطاء

### خطأ: "relation does not exist"
**الحل**: تأكد من تنفيذ ملف `supabase-setup.sql` كاملاً

### خطأ: "new row violates row-level security policy"
**الحل**: تأكد من إنشاء سياسات RLS بشكل صحيح

### خطأ في رفع الصور
**الحل**: 
1. تأكد من إنشاء bucket `vehicle-images`
2. تأكد من تفعيل Public bucket
3. تأكد من إضافة سياسات Storage

### خطأ في تسجيل الدخول بـ Google
**الحل**: 
1. تأكد من إضافة Redirect URL صحيح في Google Console
2. تأكد من تفعيل Google provider في Supabase

---

## 📊 هيكل قاعدة البيانات

```
┌─────────────────────────────────────────────────────────────┐
│                         vehicles                             │
├─────────────────────────────────────────────────────────────┤
│ id (UUID) PK                                                 │
│ user_id (UUID) FK → auth.users                              │
│ contract_no, customer_name, make, model, year               │
│ vin, plate_no, odometer, color, fuel_type                   │
│ market_value, overall_rating, recommendation                │
│ operation_status, warehouse_id, warehouse_name              │
│ evaluator_id, evaluator_name, evaluator_employee_id         │
│ gps_latitude, gps_longitude, gps_map_url                    │
│ notes, images[], created_at, updated_at                     │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│                        warehouses                            │
├─────────────────────────────────────────────────────────────┤
│ id (UUID) PK                                                 │
│ user_id (UUID) FK → auth.users                              │
│ name, location, capacity                                     │
│ manager_name, manager_phone                                  │
│ created_at, updated_at                                       │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│                        evaluators                            │
├─────────────────────────────────────────────────────────────┤
│ id (UUID) PK                                                 │
│ user_id (UUID) FK → auth.users                              │
│ name, employee_id, phone                                     │
│ created_at, updated_at                                       │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│                        activities                            │
├─────────────────────────────────────────────────────────────┤
│ id (UUID) PK                                                 │
│ user_id (UUID) FK → auth.users                              │
│ type, details (JSONB)                                        │
│ created_at                                                   │
└─────────────────────────────────────────────────────────────┘
```

---

## 📞 الدعم

إذا واجهت أي مشاكل:
1. راجع [Supabase Documentation](https://supabase.com/docs)
2. تحقق من [Supabase Discord](https://discord.supabase.com)
3. افتح issue في مستودع المشروع

---

## ✅ قائمة التحقق النهائية

- [ ] تنفيذ ملف `supabase-setup.sql`
- [ ] إنشاء bucket `vehicle-images`
- [ ] إعداد سياسات Storage
- [ ] تفعيل Email provider
- [ ] (اختياري) إعداد Google provider
- [ ] اختبار إنشاء حساب جديد
- [ ] اختبار تسجيل الدخول
- [ ] اختبار إضافة مركبة جديدة
- [ ] اختبار رفع الصور
- [ ] اختبار تصدير Excel

---

**🎉 مبروك! النظام جاهز للاستخدام**
